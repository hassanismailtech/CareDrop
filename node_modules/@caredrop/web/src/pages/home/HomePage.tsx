import { Link } from 'react-router-dom';
import { tokens, cardStyle } from '../../shared/styles';

export const HomePage = () => {
  return (
    <div style={{ display: 'grid', gap: 24, maxWidth: 1180, margin: '0 auto' }}>
      <section style={{ display: 'grid', gap: 24, gridTemplateColumns: '1.3fr 0.9fr', alignItems: 'center' }}>
        <div>
          <p style={{ margin: 0, color: tokens.colors.primary, fontWeight: 700, letterSpacing: '0.08em', textTransform: 'uppercase' }}>Fast, trusted delivery</p>
          <h1 style={{ fontSize: 42, lineHeight: 1.1, margin: '10px 0 12px', color: tokens.colors.text }}>Your pharmacy, delivered with care.</h1>
          <p style={{ margin: 0, color: tokens.colors.textMuted, fontSize: 18, maxWidth: 620 }}>
            Search real medicines, upload prescriptions, and get trusted delivery from licensed pharmacies near you.
          </p>
          <div style={{ display: 'flex', gap: 12, marginTop: 24 }}>
            <Link to="/auth" style={{ textDecoration: 'none', background: tokens.colors.primary, color: '#fff', padding: '12px 18px', borderRadius: tokens.radius.sm, fontWeight: 700 }}>Get started</Link>
            <Link to="/dashboard" style={{ textDecoration: 'none', background: tokens.colors.surface, color: tokens.colors.text, padding: '12px 18px', borderRadius: tokens.radius.sm, fontWeight: 700, border: `1px solid ${tokens.colors.border}` }}>View dashboard</Link>
          </div>
        </div>

        <div style={{ ...cardStyle, display: 'grid', gap: 12 }}>
          <div style={{ background: tokens.colors.surfaceAlt, borderRadius: tokens.radius.md, padding: 16 }}>
            <p style={{ margin: 0, color: tokens.colors.textMuted }}>Need medicine today?</p>
            <h3 style={{ margin: '8px 0 0', fontSize: 22 }}>Search by drug name or category</h3>
          </div>
          <div style={{ display: 'grid', gap: 10 }}>
            <div style={{ border: `1px solid ${tokens.colors.border}`, borderRadius: 12, padding: 12 }}>Paracetamol</div>
            <div style={{ border: `1px solid ${tokens.colors.border}`, borderRadius: 12, padding: 12 }}>Insulin</div>
            <div style={{ border: `1px solid ${tokens.colors.border}`, borderRadius: 12, padding: 12 }}>Amoxicillin</div>
          </div>
        </div>
      </section>

      <section style={{ display: 'grid', gap: 16, gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))' }}>
        {['Licensed pharmacies', 'Prescription safe', 'Fast tracking'].map((item) => (
          <div key={item} style={{ ...cardStyle }}>
            <h4 style={{ marginTop: 0, marginBottom: 8 }}>{item}</h4>
            <p style={{ margin: 0, color: tokens.colors.textMuted }}>Built to support trusted and reliable medicine delivery.</p>
          </div>
        ))}
      </section>
    </div>
  );
};
